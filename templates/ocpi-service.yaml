{{- if .Values.ocpi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "evtivity-csms.fullname" . }}-ocpi
  labels:
    {{- include "evtivity-csms.componentLabels" (dict "context" . "component" "ocpi") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.ocpi.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "evtivity-csms.componentSelectorLabels" (dict "context" . "component" "ocpi") | nindent 4 }}
{{- end }}
